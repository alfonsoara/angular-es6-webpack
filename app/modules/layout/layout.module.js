import HeaderController from './controllers/header.controller';

let layoutModule = angular.module('demo.layout', []);

layoutModule.controller('HeaderController', HeaderController);

export default layoutModule = layoutModule.name
